# n8n配置（n8n Configuration）
# 以下是n8n工作流自动化平台的基本配置参数

# 基本认证用户名 - 用于登录n8n界面
N8N_BASIC_AUTH_USER=your_username

# 基本认证密码 - 建议使用强密码保护您的n8n实例
N8N_BASIC_AUTH_PASSWORD=your_secure_password

# n8n主机地址 - 默认为localhost，如需远程访问请修改
N8N_HOST=localhost

# n8n端口号 - 默认为5678，可根据需要修改
N8N_PORT=5678

# n8n协议 - http或https，生产环境建议使用https
N8N_PROTOCOL=http

# n8n加密密钥 - 用于加密工作流中的凭证，请使用随机字符串
# 重要：一旦设置后请勿更改，否则将无法访问已保存的凭证
N8N_ENCRYPTION_KEY=your_random_encryption_key

# 时区设置（台北）- 影响工作流的调度和时间相关操作
GENERIC_TIMEZONE=Asia/Taipei

# 自动更新配置（Watchtower Configuration）
# Watchtower用于自动更新Docker容器

# 更新检查间隔（秒），默认为86400秒（24小时）
# 可根据需要调整，较小的值会更频繁地检查更新
WATCHTOWER_INTERVAL=86400